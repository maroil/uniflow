# Uniflow CDP Configuration
# Generated by `uniflow init` — version-controlled by user
version: "0.1"
region: us-east-1
adminEmail: admin@example.com
retentionDays: 90
connectors:
  - webhook
  - s3-export
